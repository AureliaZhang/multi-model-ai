{
  "name": "assistance",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "_comment_test": "placeholder; no app test suite yet (see framework Phase 6)"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "playwright": "^1.60.0"
  },
  "_comment": "Root package is ONLY for optional playwright tooling. App runtime is client/ + server/. Not the multi-model app entrypoint."
}
